@model IEnumerable<Maersk_Line.Models.Warehouse>

@{
    ViewBag.Title = "Warehouse";
}

<h2>Warehouse</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.WarehouseName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Supervisor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NumberOfContainersStored)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.WarehouseName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Supervisor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NumberOfContainersStored)
        </td>
        <td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.WarehouseID })
        <td>
            @Html.ActionLink("Details", "Details", new { id = item.WarehouseID })
        <td>
            @Html.ActionLink("Delete", "Delete", new { id = item.WarehouseID })
        </td>
    </tr>
}

</table>
